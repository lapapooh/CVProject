@*@using INTRANET.Models
    @model  List<HrEmployeeDocumentListVM>




    <h2>@Model.First().EmployeeName - Document list</h2>
    <div class="table-hover">
    <table class="table">
        <tr>
            <th>
                Title
            </th>

            <th>
               Actions
            </th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Title)
                </td>
                <td>
                <div>
                    <button type="button" class="btn btn-success" aria-label="Left Align"  onclick="location.href='@Url.Action("DownloadDocument", "HrEmployeeDocument", new {DocumentId=item.Id})'">
                        <span class="glyphicon glyphicon-save" aria-hidden="true"></span>
                    </button>


                    <button type="button" class="btn btn-danger" aria-label="Left Align"  onclick="location.href='@Url.Action("DeleteDocument", "HrEmployeeDocument", new {DocumentId=item.Id, EmployeeId=item.EmployeeId})'" >

                        <span class="glyphicon glyphicon-trash" aria-hidden="true"></span>
                    </button>
                </div>
            </td>

        </tr>

        }

    </table>
    </div>

    @*
        @section scripts{
         <script type="text/javascript">
             function DoComfirm()
          {
                if (confirm('Do you want to delete this file?')) {
                      window.location.href = "@Url.Action("DeleteDocument", "HrEmployeeDocument", new {DocumentId= documentId, EmployeeId= employeeId })";
                  }
          }
          </script>
          }*@

@using (Html.BeginForm("UploadDocument", "HrEmployeeDocument", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    <div class="container">
        <div class="row">
            <div class="form-group col-lg-12">
                <label for="documentTitle">Document title:</label>
                <input type="text" class="form-control" id="documentTitle" name="documentTitle">
                <input type="hidden" id="employeeId" name="employeeId" value="2">
            </div>
            <div class="form-group col-lg-12">
                <input type="file" class="custom-file-input" id="fileItem" name="fileItem">
            </div>
            <div class="form-group col-lg-12">
                <input type="submit" class="btn btn-primary" value="Upload" />
            </div>
        </div>
    </div>
}